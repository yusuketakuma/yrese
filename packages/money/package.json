{
  "name": "@yrese/money",
  "version": "0.0.1",
  "private": true,
  "description": "金額/点数の整数・任意精度decimal型基盤。floating pointを禁止し、政策丸め値は外部から明示注入する。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json --outDir dist"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
