{
  "name": "@yrese/audit",
  "version": "0.0.1",
  "private": true,
  "description": "監査イベント registry と AuditEvent envelope 骨格。transport/DB 非依存。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json --outDir dist"
  },
  "dependencies": {
    "@yrese/events": "workspace:*",
    "@yrese/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
