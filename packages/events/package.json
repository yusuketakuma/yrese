{
  "name": "@yrese/events",
  "version": "0.0.1",
  "private": true,
  "description": "Outbox/Inbox 前提の sync event envelope 型基盤。transport/DB 非依存。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json --outDir dist"
  },
  "dependencies": {
    "@yrese/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
