{
  "name": "@yrese/trace",
  "version": "0.0.1",
  "private": true,
  "description": "calculation_trace / legal_trace / evidence 型基盤。算定根拠のない請求影響stepを拒否する。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json --outDir dist"
  },
  "dependencies": {
    "@yrese/shared-kernel": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
