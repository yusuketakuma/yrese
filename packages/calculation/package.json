{
  "name": "@yrese/calculation",
  "version": "0.0.1",
  "private": true,
  "description": "算定エンジンの純粋関数基盤。CAL-004承認済みの初期evidence-backedルールと根拠trace配線を提供する。",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json --outDir dist"
  },
  "dependencies": {
    "@yrese/date-time": "workspace:*",
    "@yrese/money": "workspace:*",
    "@yrese/shared-kernel": "workspace:*",
    "@yrese/trace": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
