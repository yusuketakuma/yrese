/**
 * 月次締め画面(プレースホルダー)。
 * 請求月単位の締めと請求データロック。NORMAL モードでのみ許可(shared-kernel allowsClaimFinalization)。
 */
export default function Page() {
  return (
    <>
      <h2>月次締め</h2>
      <p className="placeholder-note">
        本画面は未実装のプレースホルダーです。請求月単位の締めと請求データロック。NORMAL モードでのみ許可(shared-kernel allowsClaimFinalization)。
      </p>
    </>
  );
}
