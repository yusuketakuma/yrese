# Prescription launch merge gate

The redundant patient identifier has been removed from the workflow URL. This bounded UI slice is eligible for merge when its tests, browser gate, and independent review pass.

Durable prescription storage remains explicitly out of scope and requires separately approved storage and writer authority before implementation.
